# Dependencias
node_modules/
.expo/
.expo-shared/

# Entorno (nunca subir credenciales)
.env
*.local
# google-services.json en la raíz se ignora (duplicado innecesario)
/google-services.json
# El de mobile/ SÍ se versiona para que EAS Build compile el APK
GoogleService-Info.plist
*-firebase-adminsdk-*.json

# Build outputs
dist/
build/
.next/

# Editor y sistema
.DS_Store
*.log
.vscode/
.idea/
Thumbs.db

# Claude Code (configuración local del IDE, no compartir)
.claude/

# Expo
*.orig.*
web-build/
